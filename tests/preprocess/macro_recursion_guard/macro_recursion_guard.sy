int SELF = 4;
int FIRST = 6;

int LOOP(int x) {
    return x;
}

#define SELF SELF
#define FIRST SECOND
#define SECOND FIRST
#define LOOP(x) LOOP(x)

int keep_self = SELF;
int keep_pair = FIRST;
int keep_call = LOOP(7);

int main() {
    return keep_self + keep_pair + keep_call;
}
