#if !defined(__has_feature) || !__has_feature(modules)
int feature_probe_branch = 1;
#else
int feature_probe_branch = -1;
#endif

#if !__has_extension(gnu_asm)
int extension_probe_branch = 2;
#else
int extension_probe_branch = -2;
#endif

#if !__has_builtin(__builtin_shufflevector)
int builtin_probe_branch = 3;
#else
int builtin_probe_branch = -3;
#endif

#if !__has_attribute(noescape)
int attribute_probe_branch = 4;
#else
int attribute_probe_branch = -4;
#endif

#if !__building_module(_Builtin_stddef)
int building_module_branch = 5;
#else
int building_module_branch = -5;
#endif
