#define __ASMNAME(cname) cname
#define __REDIRECT_NTH(name, proto, alias) name proto __asm__ (__ASMNAME (#alias))

extern int __REDIRECT_NTH(strerror_r,
                          (int errnum, char *buf, unsigned long buflen),
                          __xpg_strerror_r);

int main(void) {
    return 0;
}
