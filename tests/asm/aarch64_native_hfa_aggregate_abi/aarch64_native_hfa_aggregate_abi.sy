struct Pair {
    double left;
    double right;
};

struct Pair keep_pair(struct Pair value) {
    return value;
}

double use_pair(struct Pair value) {
    keep_pair(value);
    return value.right;
}
