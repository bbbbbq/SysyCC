struct Pair {
    long long left;
    long long right;
};

struct Pair keep_pair(struct Pair value) {
    return value;
}

long long use_pair(struct Pair value) {
    struct Pair tmp;
    tmp = keep_pair(value);
    return tmp.left + tmp.right;
}
