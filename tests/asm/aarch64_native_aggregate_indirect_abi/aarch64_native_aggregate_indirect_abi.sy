struct Big {
    int a;
    int b;
    int c;
    int d;
    int e;
};

struct Big keep_big(struct Big value) {
    return value;
}

int use_big(struct Big value) {
    keep_big(value);
    return value.e;
}
