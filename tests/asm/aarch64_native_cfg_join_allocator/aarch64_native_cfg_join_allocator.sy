int choose_mix(int a, int b, int c, int d, int e, int f, int g, int h) {
    int bias;
    if (a > b) {
        bias = c + d;
    } else {
        bias = e + f;
    }
    return a + b + c + d + e + f + g + h + bias;
}

int main() {
    return choose_mix(1, 2, 3, 4, 5, 6, 7, 8);
}
