int inc(int x) { return x + 1; }

int heavy(int (*fp)(int), int *p, int a, int b, int c, int d, int e) {
    int x0 = p[a];
    int x1 = p[b];
    int x2 = p[c];
    int x3 = p[d];
    int x4 = p[e];
    return x0 + x1 + fp(x2 + x3) + x4;
}

int main() { return 0; }
