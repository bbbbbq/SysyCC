struct Box {
    int value;
};

union Payload {
    struct Box box;
    unsigned long long wide;
};

static union Payload g_payload[2][1] = {{{{7}}}, {{{9}}}};

int main() {
    return g_payload[1][0].box.value;
}
