const int N = 1024;

int A[N][N];
int X[N];

int reduce_runtime_mul(int n) {
    int j = 1;
    int acc = A[3][3];
    while (j < n) {
        acc = acc - A[3][j] * X[j];
        j = j + 1;
    }
    return acc;
}

int main() {
    int n = getint();
    return reduce_runtime_mul(n);
}
