struct Pair {
    int left;
    int right;
};

int main() {
    int values[2];
    struct Pair pair;
    int index = 1;
    values[0] = 3;
    pair.left = values[0];
    values[index] = 5;
    pair.right = values[index];
    return pair.left + pair.right;
}
