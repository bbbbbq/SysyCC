typedef struct State State;

struct State {
    int value;
};

int read_value(State *state) {
    return state->value;
}

int main(void) {
    struct State state;
    state.value = 7;
    return read_value(&state) == 7 ? 0 : 1;
}
