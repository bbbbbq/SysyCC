typedef int regmatch_t;
typedef unsigned long size_t;

extern int regexec(const char *__restrict text, size_t nmatch,
                   regmatch_t pmatch[__restrict nmatch], int flags);

extern int consume_static(int values[static __restrict 4]);

int main(void) {
    return 0;
}
