typedef enum {
    FIELD_INT,
    FIELD_STR
} cmp_type;

int compare(int flag) {
    cmp_type cmp_type = FIELD_INT;
    if (flag) {
        cmp_type = FIELD_STR;
    } else if (cmp_type == FIELD_STR) {
        return 1;
    }
    return cmp_type == FIELD_STR;
}

cmp_type global_cmp;
