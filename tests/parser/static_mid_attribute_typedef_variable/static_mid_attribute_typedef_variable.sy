typedef void (*report_fn)(const char *);

void usage_builtin(const char *message) {
    (void)message;
}

static __attribute__((__noreturn__)) report_fn usage_routine = usage_builtin;

int main(void) {
    usage_routine("fatal");
    return 0;
}
