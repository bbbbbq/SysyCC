typedef unsigned int local_size_t;

typedef struct Buffer {
    char space[((unsigned int)(60 + 64 + 95))];
    local_size_t size;
} Buffer;

void init(Buffer *buffer) {
    buffer->size = sizeof(buffer->space);
}
