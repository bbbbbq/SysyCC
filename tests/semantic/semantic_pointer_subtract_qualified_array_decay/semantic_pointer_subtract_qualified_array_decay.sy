typedef unsigned int Instruction;

struct Proto {
    Instruction code[4];
};

int pc_relative(const Instruction *pc, const struct Proto *proto) {
    return (int)(pc - proto->code);
}

int main(void) {
    return 0;
}
