typedef union Value Value;

struct State {
    Value *stack;
};

union Value {
    int tag;
    double number;
};

int read_tag(struct State *state) {
    return sizeof *state->stack > 0 ? state->stack->tag : 0;
}
