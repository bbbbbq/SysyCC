void assert_like(int verbose) {
    ((void) sizeof((!verbose && "--column and --verbose are incompatible") ? 1 : 0));
}

int git_assert_like(void) {
    return !"Invalid existing change recorded";
}

int string_condition(void) {
    return "nonnull string" ? 1 : 0;
}

int string_if_condition(void) {
    if ("nonnull string") {
        return 1;
    }
    return 0;
}
