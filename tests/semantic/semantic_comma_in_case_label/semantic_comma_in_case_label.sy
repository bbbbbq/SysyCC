// Test: Comma operator in constant expression context (case label)
// Bug coverage: Comma operator missing from constant evaluation path
// The comma operator result should be the right operand

int main(void) {
    int value = 5;
    switch (value) {
    case (1, 2, 5):
        return 0;
    default:
        return 1;
    }
    return 2;
}
