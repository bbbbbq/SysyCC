struct box {
    int value;
};

int bump(struct box *box) {
    return __extension__ ({
        struct box *__o = box;
        int __delta = 1;
        __o->value += __delta;
        __o->value;
    });
}
