void *allocate_block(int size) __attribute__((__malloc__));
void fatal_error(void) __attribute__((__noreturn__));
int old_api(void) __attribute__((__deprecated__));
int print_api(const char *fmt, ...) __attribute__((format(printf, 1, 2)));
const char *translate(const char *msgid) __attribute__((format_arg(1)));
int run_many(const char *cmd, ...) __attribute__((sentinel));
int must_use(void) __attribute__((warn_unused_result));
static int helper_unused(void) __attribute__((__unused__));

void *allocate_block(int size) {
    return (void*)0;
}

void fatal_error(void) {
    while (1) {
    }
}

int old_api(void) {
    return 7;
}

int main(void) {
    return allocate_block(4) == (void*)0 && old_api() == 7 ? 0 : 1;
}
