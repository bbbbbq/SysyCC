struct dir_struct {
    enum {
        DIR_SHOW_IGNORED = 1 << 0,
        DIR_COLLECT_IGNORED = 1 << 4
    } flags;
};

int main(void) {
    struct dir_struct dir;
    dir.flags = DIR_COLLECT_IGNORED;
    return dir.flags == 16 ? 0 : 1;
}
