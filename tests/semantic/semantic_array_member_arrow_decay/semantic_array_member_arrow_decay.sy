struct AuxDb {
    const char *zDbFilename;
};

struct ShellState {
    struct AuxDb aAuxDb[5];
};

int main(void) {
    struct ShellState data;
    const char *name = "main.db";
    data.aAuxDb->zDbFilename = name;
    return data.aAuxDb->zDbFilename == name ? 0 : 1;
}
