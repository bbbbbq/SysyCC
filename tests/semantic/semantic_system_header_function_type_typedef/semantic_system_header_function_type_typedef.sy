typedef long compat_ssize_t;
typedef unsigned long compat_size_t;

typedef compat_ssize_t compat_read_function_t(void *cookie, char *buf,
                                              compat_size_t size);

compat_read_function_t *reader;

int main(void) {
    return reader == 0 ? 0 : 1;
}
