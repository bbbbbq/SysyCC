int parse_color_fields(int value) {
    enum { EXPECT_DATE, EXPECT_COLOR } next = EXPECT_COLOR;

    switch (next) {
    case EXPECT_DATE:
        return value + 1;
    case EXPECT_COLOR:
        next = EXPECT_DATE;
        break;
    }

    return next == EXPECT_DATE ? 0 : 1;
}
