struct TmpFile {
    int fd;
    int mode;
};

int main(void) {
    struct TmpFile tmp = { 0 };
    tmp = ((struct TmpFile) { .fd = -1, .mode = 2, });
    return tmp.fd == -1 && tmp.mode == 2 ? 0 : 1;
}
