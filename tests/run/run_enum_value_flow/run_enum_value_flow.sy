enum Color {
    RED = 3,
    BLUE = 7
};

enum Color g_color = BLUE;

int delta(enum Color value) {
    return value - RED;
}

int main(void) {
    putint(delta(g_color));
    putch(10);
    return 0;
}
