int main() {
    int front = getint();
    int middle = getint();
    int back = getint();
    int count = 3;

    if (count > 0) {
        putint(front);
        putch(10);
        count = 2;
    }
    if (count > 1) {
        putint(back);
        putch(10);
        count = 1;
    }
    if (count > 0) {
        putint(middle);
        putch(10);
        count = 0;
    }

    return 0;
}
