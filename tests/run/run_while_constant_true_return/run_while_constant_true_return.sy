int test(void) {
    int flow = 7;

    while (1) {
        int x = 0;
        if (x == 0) {
            return flow;
        }
        flow = flow + x;
    }
}

int main(void) {
    putint(test());
    putch(10);
    return 0;
}
