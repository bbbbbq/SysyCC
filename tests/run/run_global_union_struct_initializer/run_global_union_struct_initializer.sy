struct Box {
    int value;
};

union Payload {
    struct Box box;
    unsigned long long wide;
};

static union Payload g_payload = {{7}};

int main() {
    putint(g_payload.box.value);
    putch(10);
    return 0;
}
