int main() {
    int slot0 = 0;
    int slot1 = 0;
    int slot2 = 0;
    int top = 0;

    slot0 = getint();
    top = 1;
    slot1 = getint();
    top = 2;
    slot2 = getint();
    top = 3;

    if (top == 3) {
        putint(slot2);
        putch(10);
        top = 2;
    }
    if (top == 2) {
        putint(slot1);
        putch(10);
        top = 1;
    }
    if (top == 1) {
        putint(slot0);
        putch(10);
        top = 0;
    }

    return 0;
}
