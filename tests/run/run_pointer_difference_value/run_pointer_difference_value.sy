struct Triple {
    int first;
    int second;
    int third;
};

int distance(int *lhs, int *rhs) {
    return lhs - rhs;
}

int main() {
    struct Triple triple;
    putint(distance(&triple.third, &triple.first));
    putch(10);
    return 0;
}
