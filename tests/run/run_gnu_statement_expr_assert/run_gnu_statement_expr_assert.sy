extern void abort(void);

int checked_add_one(int value) {
    ((void)sizeof((value > 0) ? 1 : 0),
     __extension__ ({ if (value > 0) ; else abort(); }));
    return value + 1;
}

int main(void) {
    if (checked_add_one(41) != 42) {
        return 1;
    }
    return 0;
}
