int main() {
    int a = getint();
    int b = getint();
    int c = getint();
    int first = 0;
    int second = 0;
    int third = 0;

    if (a >= b && a >= c) {
        first = a;
        if (b >= c) {
            second = b;
            third = c;
        } else {
            second = c;
            third = b;
        }
    } else {
        if (b >= a && b >= c) {
            first = b;
            if (a >= c) {
                second = a;
                third = c;
            } else {
                second = c;
                third = a;
            }
        } else {
            first = c;
            if (a >= b) {
                second = a;
                third = b;
            } else {
                second = b;
                third = a;
            }
        }
    }

    putint(first);
    putch(10);
    putint(second);
    putch(10);
    putint(third);
    putch(10);
    return 0;
}
