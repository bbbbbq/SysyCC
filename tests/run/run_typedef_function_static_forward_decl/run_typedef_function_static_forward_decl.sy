typedef int reply_callback(int value);

static reply_callback handle_reply;

static int handle_reply(int value) {
    return value + 1;
}

int main(void) {
    return handle_reply(41) == 42 ? 0 : 1;
}
