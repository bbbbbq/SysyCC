float __builtin_fabsf(float value);
float __builtin_inff(void);

int main() {
    int score = 0;
    if (__builtin_fabsf(-3.5f) > 3.0f) {
        score = score + 1;
    }
    if (__builtin_inff() > 1.0f) {
        score = score + 1;
    }
    putint(score);
    putch(10);
    return 0;
}
