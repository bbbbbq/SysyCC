struct Pair {
    int first;
    int second;
};

int read_second(int *p) {
    return p[1];
}

int main() {
    struct Pair pair;
    int *p;
    pair.first = getint();
    pair.second = getint();
    p = &pair.first;
    putint(read_second(p));
    putch(10);
    return 0;
}
