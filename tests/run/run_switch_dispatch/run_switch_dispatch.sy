int main() {
    int value = getint();
    switch (value) {
    case 0: {
        putint(10);
        break;
    }
    case 1: {
        putint(20);
        break;
    }
    case 2: {
        putint(30);
        break;
    }
    default: {
        putint(99);
        break;
    }
    }
    putch(10);
    return 0;
}
