int update(int x) {
    x >>= 1;
    x <<= 2;
    x |= 3;
    x &= 14;
    x ^= 7;
    x += 5;
    x -= 1;
    x *= 2;
    x /= 3;
    x %= 4;
    return x;
}

int main() {
    putint(update(8));
    putch(10);
    return 0;
}
