struct Box {
    int value;
};

union Payload {
    struct Box box;
    unsigned long long wide;
};

static union Payload g_payload[2][1] = {{{{7}}}, {{{9}}}};

int main() {
    putint(g_payload[0][0].box.value);
    putch(32);
    putint(g_payload[1][0].box.value);
    putch(10);
    return 0;
}
