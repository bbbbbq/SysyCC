typedef struct {
    __darwin_rune_t lower;
    __uint32_t types[2];
} RuneBox;

RuneBox box;

int main(void) {
    box.lower = 7;
    box.types[1] = 35;
    putint(box.lower + (int)box.types[1]);
    putch(10);
    return 0;
}
