int read_at(int *base, unsigned long index) {
    return base[index];
}

int main() {
    int values[3] = {4, 5, 6};
    unsigned long index = 2UL;
    putint(read_at(values, index));
    putch(10);
    return 0;
}
