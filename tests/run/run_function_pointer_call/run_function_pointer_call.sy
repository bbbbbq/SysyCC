int inc(int x) {
    return x + 1;
}

int apply(int (*fn)(int), int value) {
    return fn(value);
}

int main(void) {
    int (*fn)(int) = inc;
    putint(apply(fn, 4));
    putch(10);
    return 0;
}
